import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex:1,
    // justifyContent:"center",
  },

  staffContainer: {
    position: "absolute",
    width: "98%",
    alignSelf: "center",
    height: "60%",
    flexDirection: "row",
  },
});