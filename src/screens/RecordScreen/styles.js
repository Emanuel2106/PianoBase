import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex:1,
  },

  staffContainer: {
    position: "absolute",
    height: "60%",
    width: "100%",
    flexDirection: "row",
  },
});